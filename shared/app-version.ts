/**
 * Application version — single source of truth for UI display.
 * Keep in sync with package.json "version" and git release tags.
 */
export const APP_VERSION = "1.0.6";

export const APP_VERSION_LABEL = `v${APP_VERSION}`;
